type RegistrationSteps = {
  [key: string]: string;
};

export const registrationSteps: RegistrationSteps = {
  SIGN_UP: 'signUp',
  SIGN_IN: 'signIn',
};

type Sizes = {
  SMALL: string;
  MEDIUM: string;
  LARGE: string;
};

export const sizes: Sizes = {
  SMALL: 'small',
  MEDIUM: 'medium',
  LARGE: 'large',
};
