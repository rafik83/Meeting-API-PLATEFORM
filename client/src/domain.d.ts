export type Crendentials = {
  username: string;
  password: string;
};

export type User = {
  firstName: string;
  lastName: string;
  password?: string;
  id: number;
  email: string;
};

export type Community = {
  name: string;
};
