# Read the documentation at https://github.com/thephpleague/flysystem-bundle/blob/master/docs/1-getting-started.md
flysystem:
    storages:
        companyLogos.storage:
            adapter: 'local'
            options:
                directory: '%kernel.project_dir%/public/storage/test/companyLogos'
        accountAvatar.storage:
            adapter: 'local'
            options:
                directory: '%kernel.project_dir%/public/storage/test/accountAvatar'
